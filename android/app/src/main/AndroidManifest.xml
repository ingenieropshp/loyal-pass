<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ── Agregado para geofencing en 2do plano con @capgo/background-geolocation ── -->

    <!-- Android 9+ (API 28+): declarar que la app corre un foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 14+ (API 34+): desde esta versión hay que declarar el TIPO
         específico de foreground service además del genérico de arriba;
         sin esta línea el tracking en 2do plano falla solo en Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Android 13+ (API 33+): las notificaciones locales requieren permiso runtime -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- FIX (raíz de "No pudimos cargar los restaurantes" reapareciendo incluso
         tras desinstalar/reinstalar): allowBackup="true" le permite a Android
         (y en particular a MIUI/Xiaomi, más agresivo con esto) guardar una
         copia de TODOS los datos de la app — incluyendo el localStorage con
         la sesión de Supabase — y RESTAURARLA sola al reinstalar. Por eso
         "desinstalar e instalar de nuevo" nunca arrancaba realmente en
         limpio: volvía un token de sesión viejo/vencido, que fallaba al
         intentar renovarse solo apenas se abría la app (confirmado en los
         logs del servidor: 400 en /auth/v1/token?grant_type=refresh_token
         seguido de 401 en otras peticiones). Con allowBackup="false", cada
         reinstalación real arranca sin ningún dato heredado. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
